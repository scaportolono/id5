// random

// timelimit 
let timelimit = (time) => {
}

// マップ画像の縮小比率
let ratio = () => {
}

// スポーン位置の描画
let spawn = () => {
    draw(0,0)
}

// クリックイベント
let mapClick = (e) => {
    // 座標とって返す
    return 0
}

// _h,_sの座標設定
let draw = (x,y) => {
}

// load
// マップの選出（軍需工場）
// -> 選択以外はdisplay:none
//
// map選択
// マップの選出（value=5なら全マップからランダム)
// -> 選択以外はdisplay:none
//
// start 
// startボタンのdisabled
//
// マップの選出（ランダムの場合)
// -> 選択以外はdisplay:none
//
// パターンの選出
// expectedの描画（display;Noneのまま)
// answer_hunter描画
//
// カウントダウン開始
// 画像クリックでその位置に印をつける（4回まで)
// -> answerの座標設定
// カウントダウンが終わるか、クリック数が４回終わるかで終了
//
// クリックイベントの廃棄
// クリックカウントのリセット
// カウントダウンが途中なら破棄
// expectedをdisplay:blockに

console.info(data)
